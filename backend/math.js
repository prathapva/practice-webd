const sum = (a,b)=> a+b;
const minus = (a,b)=> a-b;
const mul = (a,b)=> a*b;

const g = 9.8;

const pi = 3.14;

let obj = {
    sum : sum,
    minus : minus,
    mul : mul,
    pi : pi,
    g : g

}

module.exports = obj;